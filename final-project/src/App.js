import './App.css'
import Header from './Components/Header'
import Merge from './Components/Merge'

const App = (props) => {
  return (
    <div>
      <Header></Header>
      <Merge></Merge>
    </div>
  );
};

export default App;
